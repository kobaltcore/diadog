name: Build

on: [push]

jobs:
  upload-release:
    if: startsWith(github.ref, 'refs/tags/')
    needs: [build-linux-windows, build-macos]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Download Artifacts
        uses: actions/download-artifact@v2
        with:
          name: widow
      - name: Create Release
        uses: softprops/action-gh-release@v0.1.7
        with:
          files: '*.zip'

  build-linux-windows:
    runs-on: ubuntu-latest
    container: nimlang/nim
    steps:
      - uses: actions/checkout@v2
      - name: Build Project
        run: |
          apt-get update
          DEBIAN_FRONTEND=noninteractive apt-get install -y zip upx libgtk-3-dev libdbus-1-dev
          nimble build_all_linux
      - name: Zip Artifacts
        run: |
          zip -j widow-windows.zip bin/windows/*
          zip -j widow-linux.zip bin/linux/*
      - name: Upload Artifacts
        uses: actions/upload-artifact@master
        with:
          name: widow
          path: '*.zip'

  build-macos:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v2
      - name: Install Nim
        run: |
          brew install nim upx
      - name: Build Project
        run: |
          nimble build_all_macos
      - name: Zip Artifacts
        run: |
          zip -j widow-macos.zip bin/macos/*
      - name: Upload Artifacts
        uses: actions/upload-artifact@master
        with:
          name: widow
          path: '*.zip'
